{
  "name": "one-goal1-crore-backend",
  "version": "1.0.0",
  "description": "Production backend for OneGoal1Crore app",
  "scripts": {
    "deploy:production": "firebase deploy --only functions,firestore:rules,firestore:indexes",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "lint": "cd functions && npm run lint",
    "test": "cd functions && npm test"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "eslint": "^8.57.1",
    "firebase": "^12.0.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.6.0"
  },
  "engines": {
    "node": "22"
  }
}
